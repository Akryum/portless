{
  "name": "@portless/deamon",
  "version": "0.1.0",
  "description": "(Background deamon) Easy local domains and reverse proxy with HTTPS support",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "Guillaume Chau",
  "license": "MIT",
  "keywords": [
    "https",
    "localhost"
  ],
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "portless-deamon": "dist/bin.js"
  },
  "scripts": {
    "build": "rimraf ./dist && tsc -d && chmod u+x dist/bin.js",
    "prepublishOnly": "yarn build",
    "test": "jest"
  },
  "dependencies": {
    "@portless/config": "^0.1.0",
    "@portless/template": "^0.1.0"
  },
  "devDependencies": {
    "rimraf": "^3.0.0",
    "typescript": "^3.4.3"
  },
  "files": [
    "dist/**/*",
    "templates/**/*"
  ]
}
